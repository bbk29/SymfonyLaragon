{% extends 'base.html.twig' %}

{% block title %}Liste des Réalisateurs.trices{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { bdireckground: #F5F5F5; padding: 2px 6px; }
</style>

<h2 class="border border-dark rounded p-2 m-2 text-white bg-primary">Réalisateurs.trices</h2>

    {% for director in directors %}
        <h4> {{director.prenom}} {{director.nom}}</h4> <br>
        <a href="{{ path('affichedirector', {'id':director.id}) }}">
        <img src="{{director.photo}}" width=200><br>
        </a>
        <a href="{{ path('modifdirector', {'id':director.id}) }}">MODIFIER</a><br>
        <hr>
    {% endfor %}

{% endblock %}