{% extends 'base.html.twig' %}

{% block title %}Modif. Acteur.trice{% endblock %}

{% block body %}
<h2 class="border border-dark rounded p-2 m-2 text-white bg-primary"> Modifier {{actor.prenom}} {{actor.nom}}</h2>

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <img src="{{actor.photo }}" style="width:250px">
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}

{% endblock %}