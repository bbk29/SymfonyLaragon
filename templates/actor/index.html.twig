{% extends 'base.html.twig' %}

{% block title %}Liste des Acteurs.trices{% endblock %}

{% block body %}
<h2 class="border border-dark rounded p-2 m-2 text-white bg-primary">Acteurs.trices</h2>

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    {# <div class="example-wrapper"> #}
        {# <h1>Hello {{ controller_name }}! ✅</h1> #}

        {# This friendly message is coming from: #}
        {# <ul> #}
            {# <li>Your controller at <code><a href="{{ 'C:/laragon/www/SymfonyLaragon/src/Controller/ActorController.php'|file_link(0) }}">src/Controller/ActorController.php</a></code></li> #}
            {# <li>Your template at <code><a href="{{ 'C:/laragon/www/SymfonyLaragon/templates/actor/index.html.twig'|file_link(0) }}">templates/actor/index.html.twig</a></code></li> #}
        {# </ul> #}
    {# </div> #}

    {% for actor in actors %}
        <h4> {{actor.prenom}} {{actor.nom}}</h4>
        <br>
        <a href="{{ path('afficheactor', {'id':actor.id}) }}">
        <img src="{{(actor.photo) }}" style="width:200px">
        </a><br>
        <a href="{{ path('modifactor', {'id':actor.id}) }}">MODIFIER</a><br>
        <hr>
    {% endfor %}

{% endblock %}