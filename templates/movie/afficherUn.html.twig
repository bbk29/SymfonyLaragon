{% extends 'base.html.twig' %}

{% block title %}Un Film{% endblock %}

{% block body %}
<h2 class="border border-dark rounded p-2 m-2 text-white bg-primary">{{movie.title}}</h2>

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

        <img src="{{(movie.cover) }}" style="width:300px"><br><br>
        Genre : {{movie.genre.type}}<br><br>

        <h5>Réalisateur.trice(s) :</h5>
         {{movie.director.prenom}} {{movie.director.nom}}
        <br><br>
        <h5>Acteur.trice(s) :</h5>
        {%for actor in movie.actors %}
            - {{actor.prenom}} {{actor.nom}}<br>
        {% endfor %}<br>

        <hr>

{% endblock %}